services:
  cook-schedule:
    build: .
    image: cook-schedule:latest
    container_name: cook-schedule
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - ./data:/app/data
