package com.cigama.cook_schedule.repository;

import com.cigama.cook_schedule.entity.ApprovedRoster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApprovedRosterRepository extends JpaRepository<ApprovedRoster, Integer> {
}
